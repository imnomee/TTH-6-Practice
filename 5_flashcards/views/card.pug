extends layout.pug
block content
      section#content 
            h2= prompt 
            if hint 
                  p 
                        i Hint: #{hint}
            else 
                  p (There is not hint)
            //- ol 
            //-       each color in colors 
            //-             li= color